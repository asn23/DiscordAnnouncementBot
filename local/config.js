const config = {
    bot: {
        prefix: 'PREFIX HERE', // Put the prefix you want commands to start with here
        token: 'PUT PREFIX HERE', // Put your Discord Bot Token here
        gameName: 'PLAYING STAUS HERE', // Put the playing status you want here        
    },

    configVersion: '3' //Don't change this!
}

module.exports = config;